/**
 * Threshold de estoque baixo (uma fonte de verdade no backend).
 *
 * Espelha frontend/src/lib/constants.ts. Quando for unificado em um
 * monorepo no futuro, esses dois arquivos viram um pacote `shared`.
 */
export const LOW_STOCK_THRESHOLD = 25;
